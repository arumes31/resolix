# Auto detect text files and perform LF normalization
* text=auto

# Force LF for files that will run inside Linux containers
*.sh text eol=lf
Dockerfile text eol=lf
Dockerfile.* text eol=lf
entrypoint.sh text eol=lf
