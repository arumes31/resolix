name: Go Checks

on:
  push:
    branches: [ "main", "v2_test" ]
  pull_request:
    branches: [ "main" ]
  workflow_dispatch:

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v7

      - name: Set up Go
        uses: actions/setup-go@v7
        with:
          go-version-file: 'webgui/go.mod'
          cache-dependency-path: 'webgui/go.sum'

      - name: Run govulncheck
        run: go install golang.org/x/vuln/cmd/govulncheck@v1.6.0 && govulncheck ./...
        working-directory: webgui

      - name: Run gosec
        run: go install github.com/securego/gosec/v2/cmd/gosec@v2.28.0 && gosec ./...
        working-directory: webgui

      - name: Run golangci-lint
        run: |
          go install github.com/golangci/golangci-lint/v2/cmd/golangci-lint@v2.12.2
          $(go env GOPATH)/bin/golangci-lint run
        working-directory: webgui
